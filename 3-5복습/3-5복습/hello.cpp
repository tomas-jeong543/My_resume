////이 해더파일은 c에서의 stdio.h와 같다고 보면 된다 기본적인 표준 입출력을 위한 
////클래스와 객체 변수 등이 선언된다
//#include<iostream>
////네임 스페이스는 자바의 페키지를 생각하면 된다 일종의 동일 이름의 함수를
////구분하기 위해 쓰인다 또한 네임 스페이스 안에 네임 스페이스가 있는 것 역시 가능하다
//using namespace std;
//
//int main(void)
//{	
//	//cout함수는 내장 함수로 콘솔에 값을 출력하는 객체이다 또한 c에서 배웠지만 출력버퍼
//	//에 담아 두었다가 블록 단위로 출력하는 것
//	//담아 두는 기준은 
//	cout<< "Hello World" << endl << 1 << "and" << 5 + 3;
//	return 0;
//}