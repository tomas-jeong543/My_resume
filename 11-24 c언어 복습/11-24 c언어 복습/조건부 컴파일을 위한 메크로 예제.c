/*#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#define HIT_NUM  7

int main(void)
{
	#if HIT_NUM == 5
		puts("메크로 상수 HIT_NUM의 값은 현재 5입니다.");
	#elif HIT_NUM == 6
		puts("메크로 상수 HIT_NUM의 값은 현재 6입니다.");
	#elif HIT_NUM == 7
		puts("메크로 상수 HIT_NUM의 값은 현재 7입니다.");
	#else
		puts("메크로 상수 HIT_NUM의 값은 현재 5,6,7은 확싱이 아닙니다.");
	#endif
		
	return 0;
}*/